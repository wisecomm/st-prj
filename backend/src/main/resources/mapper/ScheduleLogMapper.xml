<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springrest.domain.scheduler.repository.ScheduleLogMapper">

    <resultMap id="ScheduleLogResultMap" type="com.example.springrest.domain.scheduler.model.entity.ScheduleLog">
        <id property="uid" column="uid" />
        <result property="corpCode" column="corp_code" />
        <result property="beanName" column="bean_name" />
        <result property="method" column="method" />
        <result property="result" column="result" />
        <result property="message" column="message" />
        <result property="startTime" column="start_time" />
        <result property="endTime" column="end_time" />
        <result property="worker" column="worker" />
    </resultMap>

    <!-- 실행 시작 시 로그 삽입 (진행중 I 상태) -->
    <insert id="insertLog" useGeneratedKeys="true" keyProperty="uid">
        INSERT INTO SA_SYNC_SCHEDULE_LOG (
            CORP_CODE,
            BEAN_NAME,
            METHOD,
            RESULT,
            MESSAGE,
            WORKER
        ) VALUES (
            #{corpCode},
            #{beanName},
            #{method},
            #{result},
            #{message},
            #{worker}
        )
    </insert>

    <!-- 실행 종료/실패 시 로그 업데이트 -->
    <update id="updateLog">
        UPDATE SA_SYNC_SCHEDULE_LOG
        <set>
            <if test="result != null">RESULT = #{result},</if>
            <if test="message != null">MESSAGE = #{message},</if>
            END_TIME = CURRENT_TIMESTAMP
        </set>
        WHERE UID = #{uid}
    </update>

    <!-- 전체 로그 조회 -->
    <select id="findAllLogs" resultMap="ScheduleLogResultMap">
        SELECT 
            UID, CORP_CODE, BEAN_NAME, METHOD, RESULT, MESSAGE, START_TIME, END_TIME, WORKER
        FROM SA_SYNC_SCHEDULE_LOG
        ORDER BY START_TIME DESC
    </select>

</mapper>
